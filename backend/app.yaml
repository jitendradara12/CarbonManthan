runtime: python310
env: standard
entrypoint: gunicorn -b :$PORT server.wsgi:application
handlers:
  - url: /static
    static_dir: staticfiles/
  - url: /media
    static_dir: media/
  - url: /frontend
    static_dir: ../frontend/
  - url: /.*
    script: auto
